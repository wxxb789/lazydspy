[project]
name = "lazydspy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dspy",
    "anthropic",
    "openai",
    "pydantic>=2",
    "python-dotenv",
    "rich",
]

[project.scripts]
lazydspy = "lazydspy.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lazydspy"]

[tool.hatch.build.targets.sdist]
include = ["src"]

[tool.uv]
dev-dependencies = [
    "mypy",
    "pytest",
    "ruff",
]
